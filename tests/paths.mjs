// One place that knows where the extension lives, so moving it is a
// one-line change for the whole suite.
export const EXTENSION = new URL('../modules/module-1-prompt-composer/', import.meta.url);
